<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<window xmlns="http://schemas.haulmont.com/cuba/screen/window.xsd"
        caption="msg://medicationEdit.caption"
        focusComponent="form"
        messagesPack="com.company.medicalrecord.web.screens.medication">
    <data>
        <instance id="medicationDc"
                  class="com.company.medicalrecord.entity.Medication">
            <view extends="_local"/>
            <loader/>
        </instance>
    </data>
    <dialogMode height="600"
                width="800"/>
    <layout expand="editActions" spacing="true">
        <form id="form" dataContainer="medicationDc">
            <column width="350px">
                <textField id="medicineNameField" property="medicineName"/>
                <textArea id="intakeRecommendationField" property="intakeRecommendation" rows="5"/>
            </column>
        </form>
        <hbox id="editActions" spacing="true">
            <button id="commitAndCloseBtn" action="windowCommitAndClose"/>
            <button id="closeBtn" action="windowClose"/>
        </hbox>
    </layout>
</window>
